import React from 'react'
import { View  } from 'react-native'
import NavigationFile from './navigationFile';

export default class AllInOne extends React.PureComponent {

    render() {
        return (
            <NavigationFile/>
        )
    }
}
